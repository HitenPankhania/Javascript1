// var name ="hiten";
// name = name.toUpperCase();
// name = name.toLowerCase();


// 1 Create a var that stores that save the name that user enters via prompt

var name =prompt("What is your name?");

// 2 Capatlise the first letter of their Name
var firstChar = name.slice(0.1)

// A isolate the chara
var upperCaseFirstChar = firstChar.toUpperCase()

// B Turn the first case to upperCase
var restOfName = name.slice(1.6)

// C Isolate the rest of the name 

// D concactenate the first chara to the rest of the chara



// 3 We use the capatalised version of their namee to greet the using an alert